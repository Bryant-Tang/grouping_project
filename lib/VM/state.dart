enum RegisterState {
  success,
  faild,
}
enum LoginState {
  loginSuccess,
  loginFaild,
  userNotFound,
  wrongPassword,
}
enum TagEditMode{
  create,
  edit,
  delete,
}
enum WorkspaceMode{
  personal,
  group,
}
enum EventSettingMode { 
  create, edit, displpay 
}
