enum RegisterState {
  success,
  fail,
}
enum LoginState {
  loginSuccess,
  loginFail,
  userNotFound,
  wrongPassword,
}
enum TagEditMode{
  create,
  edit,
  delete,
}
enum WorkspaceMode{
  personal,
  group,
}

// enum SettingMode { 
//   create, edit, displpay 
// }
